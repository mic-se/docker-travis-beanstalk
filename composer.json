{
    "autoload": {
        "psr-4": {
            "App\\": "src/"
        }
    },
    "require-dev": {
        "phpunit/phpunit": "^7",
        "fzaninotto/faker": "^1.8",
        "symfony/debug": "^4.2",
        "symfony/var-dumper": "^4.2"
    },
    "scripts": {
        "test": "./vendor/bin/phpunit --colors=always --verbose --bootstrap vendor/autoload.php --testdox tests/"
    }
}
